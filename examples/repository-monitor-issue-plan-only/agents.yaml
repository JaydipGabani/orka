apiVersion: core.orka.ai/v1alpha1
kind: Agent
metadata:
  name: issue-researcher
  namespace: default
spec:
  secretRef:
    name: claude-runtime-credentials
  runtime:
    type: claude
    contractVersion: orka.harness.v2
    defaultMaxTurns: 50
    defaultAllowedTools:
      - Read
      - Glob
      - Grep
  systemPrompt:
    inline: |
      Research and plan GitHub issues for Orka RepositoryMonitor. Do not mutate GitHub.
